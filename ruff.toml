line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]   # FastAPI Depends(...) в дефолтах аргументов — это норма

[lint.per-file-ignores]
"alembic/*" = ["E501"]
